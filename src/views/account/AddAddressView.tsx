import React from 'react';
import AddAddress from '../../components/Address/AddAddress';

const AddAddressView: React.FC = () => {
    return (
        <div>
            <AddAddress />
        </div>
    );
};

export default AddAddressView;